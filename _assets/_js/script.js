/*
function goToWeek() {
    var data = document.getElementById('semana').value;
    var semana = data.split('-');
    var page = semana.slice(1);
    var pageID = "#".concat(page);
    var link = "index.html".concat(pageID);
    if (data != "") {
        window.onclick.href.replace(link);
    } 
};

document.getElementById("search").addEventListener("click", goToWeek());*/